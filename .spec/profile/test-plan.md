🧩 Тест‑план: Dynamic Tables & Relations (Elbruso Platform)
📘 1. Цель тестирования
Проверить корректность работы:

динамических таблиц

структуры таблиц (колонки, строки, типы данных)

связей между таблицами

межворкспейсных связей

формул и HUD

графа зависимостей

версионирования и черновиков

прав доступа

интеграции с AI‑агентом

📘 2. Объём тестирования (Scope)
Включено:
CRUD таблиц

CRUD колонок

CRUD строк

Lookup / Join / Reference связи

Cross‑workspace связи

Формулы (HyperFormula)

HUD

Dependency Graph

Versioning

Drafts

Permissions

API

UI

AI‑агент

Не включено:
Авторизация (покрывается отдельным тест‑планом)

Справочники (отдельный модуль)

📘 3. Риски
Функциональные:
циклические зависимости

некорректный пересчёт формул

потеря данных при публикации версии

неправильная работа lookup при изменении ключей

рассинхронизация cross‑workspace данных

UX:
HUD может зависнуть или показывать неверные подсказки

пользователь может потерять контекст при ссылках

Безопасность:
утечка данных между воркспейсами

обход прав доступа через формулы

AI‑агент может изменить данные без разрешения

📘 4. Тестовые сценарии
Ниже — полный набор тест‑кейсов, сгруппированный по функциональным блокам.

🧩 A. Тесты создания таблиц
A1. Создание пустой таблицы
создать таблицу с минимальными данными

создать таблицу с длинным названием

создать таблицу с emoji

создать таблицу с пустым описанием

попытка создать таблицу без прав

A2. Создание таблицы на основе справочника
регионы РФ

критерии

организации

фильтрация по стране

справочник пуст → ошибка

A3. Создание таблицы по шаблону
шаблон с формулами

шаблон с зависимостями

шаблон с locked‑ячейками

🧩 B. Тесты структуры таблиц
B1. Колонки
добавить колонку всех типов

изменить тип колонки

удалить колонку

скрыть/показать колонку

переместить колонку drag‑and‑drop

попытка удалить колонку, используемую в формуле → ошибка

B2. Строки
добавить строку

удалить строку

массовое добавление (1000 строк)

сортировка

фильтрация

🧩 C. Тесты связей внутри воркспейса
C1. Lookup
связь по ключу 1:1

ключ отсутствует → пустая ячейка

изменение ключа → обновление lookup

удаление строки в источнике → orphan‑значение

C2. Join
left join

inner join

join по нескольким колонкам

join с большим объёмом данных

C3. Reference
выпадающий список

выбор строки из другой таблицы

изменение значения в источнике

🧩 D. Cross‑Workspace связи
D1. Read‑only связь
подключение таблицы из другого воркспейса

изменение источника → уведомление

отключение связи

D2. Агрегация
сбор данных из 10 воркспейсов

конфликт типов данных

отсутствие доступа к одному из воркспейсов

🧩 E. Формулы и HUD
E1. HUD
ввод =

подсказки функций

подсветка ячеек

переход между таблицами

отображение пути к ячейке

E2. Формулы
арифметические

логические

ссылки на другие таблицы

ссылки на другие воркспейсы

массивные формулы

формулы с ошибками

E3. Ошибки формул
циклическая зависимость

деление на ноль

ссылка на удалённую колонку

неверный тип данных

🧩 F. Dependency Graph
F1. Построение графа
формула → зависимость

lookup → зависимость

join → зависимость

F2. Пересчёт
изменение источника

массовый пересчёт

пересчёт 100+ зависимых таблиц

🧩 G. Версионирование
G1. Черновики
создание черновика

внесение изменений

сравнение diff

G2. Публикация
публикация версии

конфликт версий

публикация с ошибками формул → запрет

G3. Откат
откат к предыдущей версии

откат таблицы с зависимостями

🧩 H. Права доступа
H1. Таблицы
просмотр без прав

редактирование без прав

создание таблицы без прав

H2. Колонки
скрытые колонки

запрещённые колонки

H3. Cross‑workspace
попытка доступа к закрытому воркспейсу

🧩 I. Интеграция с AI‑агентом
I1. Создание таблицы
агент создаёт таблицу

агент создаёт черновик

I2. Предложения
агент предлагает формулу

агент предлагает связь

I3. Ограничения
агент не может публиковать версии

агент не может менять закрытые колонки

🧩 J. Нефункциональные тесты
J1. Производительность
10 000 строк

200 колонок

100 формул

50 зависимых таблиц

J2. Нагрузочные
массовый пересчёт

массовое создание таблиц

J3. UX
скорость HUD

плавность drag‑and‑drop

J4. Безопасность
SQL‑инъекции

попытка обхода прав через формулы

попытка чтения чужих воркспейсов
