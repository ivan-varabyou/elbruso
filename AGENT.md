# GLOBAL RULES
DRY, KISS, YAGNI, SOLID

# Workspaces PNPM
install packages for all workspaces: pnpm install
install packages for specific workspace: pnpm install --filter @elbruso/context

# Arhitecture
@docs - global documentation
@apps/web/ - frontend
@apps/api/ - backend
@packages/shared/ - shared logic for web app and admin panel

    
