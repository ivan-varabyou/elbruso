import {
  DynamicTables,
  TableVersions,
  TableCells,
  TableLinks,
} from '@elbruso/database';

export { DynamicTables, TableVersions, TableCells, TableLinks };

export type Table = DynamicTables;
export type TableVersion = TableVersions;
export type TableCell = TableCells;
export type TableLink = TableLinks;
